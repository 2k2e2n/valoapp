import React from "react";
import Image, { StaticImageData } from "next/image";


interface PointerProps {
    src: string; // Optional
    style: React.CSSProperties; // Optional
}

const Button: React.FC<PointerProps>  = ({src}) => {

return (
    <div>

    </div>
)}

export default Button;